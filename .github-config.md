# Repository Configuration

## Git Configuration

```bash
# Local config (already set)
git config user.name "Violet Figueroa"
git config user.email "violetdfigueroa@gmail.com"

# Push to GitHub
git remote add origin https://github.com/VioletFigueroa/conversation-to-logseq.git
git branch -M main
git push -u origin main
git push --tags origin
```

## GitHub Repository Settings

### Description
Convert Perplexity and VS Code conversations into Logseq-compatible notes with automatic domain classification, duplicate detection, and batch processing.

### Topics (Keywords)
- `logseq`
- `knowledge-management`
- `perplexity`
- `vs-code`
- `conversation-archival`
- `automation`
- `cybersecurity`
- `python-cli`

### Homepage URL
(optional - leave blank or point to docs)

### Visibility
✓ Public

### Options
✓ Discussions (enabled)
✓ Wiki (disabled)
✓ Issues (enabled)
✓ Projects (disabled)

## Protected Branches

No protection needed for initial release (single maintainer).

## Collaborators

None initially (public repository).

## Release Strategy

### Current Release: v1.0.0
- Initial stable release
- Feature complete
- Production ready
- Comprehensive documentation

### Future Releases
- Semantic versioning (SemVer)
- Tag format: `v{major}.{minor}.{patch}`
- Release notes with highlights

## Continuous Integration (Optional)

Current setup requires no CI/CD (no dependencies, no tests).

For future enhancement:
- GitHub Actions for linting
- Python code quality checks
- Automated testing
